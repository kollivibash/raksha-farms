# Dependencies
node_modules/

# Environment files (never commit secrets)
.env
.env.local
# .env.production is intentionally tracked — only contains public VITE_/NEXT_PUBLIC_ vars (no secrets)

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Build outputs
frontend/dist/
admin/.next/
admin/out/

# Claude worktrees (internal tooling)
.claude/

# Uploads (user-generated content, store on server)
backend/src/uploads/

# Misc
*.zip
raksha\ farms\ logo.zip
