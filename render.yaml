services:
  - type: web
    name: raksha-farms-backend
    runtime: node
    rootDir: backend
    branch: master
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_SECRET
        value: raksha@admin2024
